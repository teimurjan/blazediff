FROM rust:latest

WORKDIR /app

RUN apt-get update && apt-get install -y \
    cmake \
    nasm \
    zlib1g-dev \
    && rm -rf /var/lib/apt/lists/*

COPY . .

# Token passed via file mount at runtime, not build arg
CMD ["sh", "-c", "cargo publish --token \"$(cat /run/secrets/crates_token)\""]
