---
"@blazediff/agent": minor
---

Add `review` webapp (Vite + node SSR; replaces `summary.html`), local Moondream/Qwen judge with serialized per-test progress, two-phase capture→dispatch graph, and generic harness loader. The `auth` subcommand is folded into `harness record`.
